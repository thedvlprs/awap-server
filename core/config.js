module.exports = {

	/* === === === === === */
	/* Development
	/* === === === === === */

	development: {

		/* === === === === === */
		/* HTTP
		/* === === === === === */

		http: {
			ip: '0.0.0.0',
			port: 8002
		},

		/* === === === === === */
		/* Database
		/* === === === === === */

		db: {
			host: '127.0.0.1:27017',
			name: 'awap'
		}
	},

	/* === === === === === */
	/* Production
	/* === === === === === */

	production: {

		/* === === === === === */
		/* HTTP
		/* === === === === === */

		http: {
			ip: '127.0.0.1',
			port: 8002
		},

		/* === === === === === */
		/* Database
		/* === === === === === */

		db: {
			host: '127.0.0.1:27017',
			name: 'awap'
		}
	}

}